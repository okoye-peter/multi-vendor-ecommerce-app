{
  "name": "multi-vendor-e-commerce-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --include=dev --prefix backend && npm run build --prefix backend && npm install --include=dev --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "start:prod": "cd backend && node_modules/.bin/prisma migrate deploy && NODE_ENV=production node_modules/.bin/tsx src/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}